<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TaskBag.Views.AuthPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Authorization:"/>
            <Entry              
                x:Name="Email" Text="{Binding Email}" Placeholder="Email" 
                HorizontalOptions="CenterAndExpand" >
            </Entry>
            <Entry
                x:Name="Password" Text ="{Binding Password}" Placeholder="Password" IsPassword="true" >
            </Entry>

            <Button x:Name="btnLogin" Text="Login" Command="{Binding LoginCommand}" >
            </Button>
            <Button x:Name="btnSignup" Text="Sign Up" Command="{Binding SignUpCommand}" >
            </Button>

            <ActivityIndicator IsRunning="{Binding IsBusy}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>